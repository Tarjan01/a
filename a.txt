一、仍未解决的问题
预训练权重下载地址还是空的
11.1 节的 wget <sam_weight_url> 占位符没有替换，2.3 节的权重表格也只有文件名没有 URL。SAM 权重 2.5GB，找错了很浪费时间。
load_parameter 变量未定义
训练流程代码里 if epoch == free_epochs or load_parameter 中的 load_parameter 在文档任何地方都没有解释——它是命令行参数、config.yaml 里的字段，还是脚本内的硬编码 bool？
CrowdPose 数据集的获取和标签格式
文档说 rearranged_data 包含 CrowdPose 的正常样本，并且提到了 crowdpose_trainval.json，但没说：

CrowdPose 从哪里下载
它的标签格式和 HumanRefiner 不同（CrowdPose 是关键点标注，没有异常标签），转成本项目"正常样本（标签为 0）"的转换逻辑在哪个脚本里

pos_weights 的计算时机
文档描述了计算公式，但没说是在训练前对整个训练集统计一次，还是动态计算。这影响第一次运行时的行为。

二、新引入的小问题
MMCV 版本 hack 的位置描述有歧义
v2 说"代码先导入原始 mmcv 版本"，但没说这行代码需要在 import mmdet 之前执行。如果复现者的脚本里 import 顺序不对，hack 不会生效，报错信息也不明显。建议明确写出正确的 import 顺序：
pythonimport mmcv
mmcv.__version__ = '2.1.0'   # 必须在 import mmdet 之前
import mmdet
预生成缓存的命令没有独立入口
6.6 节说训练前要调用 batch_pre_generate_all_caches()，但这个函数是在 sk_resnet.py 内部调用的，还是有一个独立的脚本可以单独运行？如果直接运行 sk_resnet.py 会自动触发预生成，应该说清楚；如果需要单独运行，应该给出命令。

三、锦上添花（可选但有帮助）
这些不是硬性缺失，但加上会让文档更完整：

rearrange_data.py 期望的输入目录名是什么（data/train/、data/humanrefiner/还是其他）——目前只说"放在 data/ 目录下"
验证集 val_acc=100% 看起来有点异常，可以加一句解释（比如"248 张数据量小，且是经过筛选的高质量标注样本，所以验证集过拟合是正常的"）